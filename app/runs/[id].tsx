import React, { useEffect, useState } from 'react';
import { ActivityIndicator, ScrollView, StyleSheet, Text, View } from 'react-native';
import { useLocalSearchParams } from 'expo-router';
import { apiFetch } from '../../src/api';

export default function RunScreen() {
  const { id } = useLocalSearchParams<{ id: string }>(); const [run, setRun] = useState<any>(null); const [events, setEvents] = useState<any[]>([]); const [error, setError] = useState('');
  useEffect(() => { let active = true; async function poll() { try { const [runResponse, eventsResponse] = await Promise.all([apiFetch(`/api/v1/runs/${id}`), apiFetch(`/api/v1/runs/${id}/events`)]); if (!runResponse.ok) throw new Error('Run is not available in this tenant'); const payload = await runResponse.json(); if (active) setRun(payload.run); if (eventsResponse.ok) { const text = await eventsResponse.text(); const parsed = text.split('\n\n').map((block) => block.split('\n').find((line) => line.startsWith('data: '))).filter(Boolean).map((line) => JSON.parse(line!.slice(6))).filter((value) => value.type); if (active) setEvents(parsed); } } catch (value) { if (active) setError(value instanceof Error ? value.message : 'Could not load run'); } } void poll(); const timer = setInterval(() => void poll(), 2000); return () => { active = false; clearInterval(timer); }; }, [id]);
  if (error) return <View style={styles.container}><Text style={styles.error}>{error}</Text></View>;
  if (!run) return <View style={styles.container}><ActivityIndicator color="#38bdf8" /></View>;
  return <ScrollView style={styles.container}><Text style={styles.title}>{run.deviceName}</Text><Text style={styles.state}>{run.state.replaceAll('_', ' ')}</Text><Text style={styles.question}>{run.question}</Text><Text style={styles.section}>Durable events</Text>{events.map((event) => <View style={styles.event} key={`${event.sequence}-${event.type}`}><Text style={styles.sequence}>{event.sequence}</Text><Text style={styles.eventText}>{event.type}</Text></View>)}{run.diagnosis ? <><Text style={styles.section}>Grounded diagnosis</Text><Text style={styles.summary}>{run.diagnosis.summary}</Text><Text style={styles.muted}>{run.diagnosis.uncertainty}</Text>{run.diagnosis.citations.map((citation: any) => <Text style={styles.citation} key={citation.id}>{citation.title}: {citation.excerpt}</Text>)}</> : null}</ScrollView>;
}
const styles = StyleSheet.create({ container: { flex: 1, backgroundColor: '#090d16', padding: 16 }, title: { color: '#f8fafc', fontSize: 24, fontWeight: '700' }, state: { color: '#38bdf8', textTransform: 'uppercase', marginTop: 6 }, question: { color: '#cbd5e1', marginVertical: 14 }, section: { color: '#38bdf8', fontWeight: '700', marginTop: 18, marginBottom: 8 }, event: { flexDirection: 'row', gap: 12, borderTopWidth: 1, borderTopColor: '#334155', paddingVertical: 10 }, sequence: { color: '#38bdf8', fontFamily: 'monospace' }, eventText: { color: '#f8fafc' }, summary: { color: '#f8fafc', fontSize: 17, fontWeight: '600' }, muted: { color: '#94a3b8', marginTop: 8 }, citation: { color: '#cbd5e1', borderLeftWidth: 2, borderLeftColor: '#38bdf8', padding: 8, marginTop: 10 }, error: { color: '#fb7185' } });
